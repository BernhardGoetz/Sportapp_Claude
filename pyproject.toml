[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sportstunden-planer"
version = "3.0.0"
description = "Automatische Planung von Kinderturnstunden (1-10 Jahre) mit Stundenbild als PDF"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
sportstunden = "sportstunden.cli:main"

[project.gui-scripts]
sportstunden-gui = "sportstunden.gui:starte"

[tool.setuptools]
packages = ["sportstunden"]

[tool.setuptools.package-data]
sportstunden = ["data/*.json"]
